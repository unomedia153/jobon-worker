name: jobon_worker
description: jobon 작업자 모바일 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.0.0

  # 위치/GPS
  geolocator: ^10.1.0
  permission_handler: ^11.0.1

  # 상태관리
  flutter_riverpod: ^2.4.9
  flutter_hooks: ^0.20.5

  # 라우팅
  go_router: ^13.0.0

  # UI
  cupertino_icons: ^1.0.2

  # 유틸리티
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true